[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kg-mcp"
version = "0.2.0"
description = "Local-first activity memory and knowledge graph for people and AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Davide Leopardi"}
]
keywords = ["memory", "mcp", "knowledge-graph", "neo4j", "ai-assistant", "local-first"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "mcp>=1.0.0,<2.0.0",
    "fastapi>=0.115.0",
    "neo4j>=5.0.0",
    "litellm>=1.40.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "Pillow>=10.0.0",
    "uvicorn>=0.30.0",
]

[project.urls]
Homepage = "https://github.com/Hexecu/mcp-neuralmemory"
Documentation = "https://github.com/Hexecu/mcp-neuralmemory#readme"
Repository = "https://github.com/Hexecu/mcp-neuralmemory"
Issues = "https://github.com/Hexecu/mcp-neuralmemory/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "black>=24.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]

[project.scripts]
kg-mcp = "kg_mcp.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kg_mcp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
