{
    "servers": {
        "kg-memory": {
            "type": "stdio",
            "command": "${workspaceFolder}/server/.venv/bin/kg-mcp",
            "args": ["--transport", "stdio"]
        }
    }
}
